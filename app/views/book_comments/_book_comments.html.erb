<table class='table table-hover table-inverse'>
  <thead>
    <tr>
      <td></td>
      <td></td>
    </tr>
  </thead>
  <tbody>
    <% book.book_comments.each do |book_comment| %>
      <tr>
        <td><%= link_to(book_comment.user) do %>　
              <%= image_tag book_comment.user.get_profile_image(50,50) %><br>
              <%= link_to book_comment.user.name %>
            <% end %>
        </td>  
        <td>
          <%= book_comment.comment %>
        </td>
        <% if book_comment.user == current_user %>
          <td><%= link_to 'Destroy', book_book_comment_path(book, book_comment), method: :delete, remote: true, class: "btn btn-sm btn-danger pull-right" %></td>
        <% end %>
      </tr>
    <% end %>  
  </tbody>
</table>  